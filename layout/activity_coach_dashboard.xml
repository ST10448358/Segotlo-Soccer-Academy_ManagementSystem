<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/coach_drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/black"
    tools:context=".CoachDashboardActivity">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <!-- Main Content Area -->
        <FrameLayout
            android:id="@+id/coach_content_frame"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- Optional: Hamburger button for mobile users -->
        <ImageButton
            android:id="@+id/btn_coach_menu"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_marginStart="16dp"
            android:layout_marginTop="16dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_menu"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:tint="@color/academy_yellow" />

    </androidx.constraintlayout.widget.ConstraintLayout>

    <!-- Side Menu -->
    <com.google.android.material.navigation.NavigationView
        android:id="@+id/coach_nav_view"
        android:layout_width="240dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:background="#0D0D0D"
        app:itemIconTint="@color/academy_yellow"
        app:itemTextColor="@color/white"
        app:menu="@menu/coach_drawer_menu"
        app:headerLayout="@layout/nav_header_coach" />

</androidx.drawerlayout.widget.DrawerLayout>